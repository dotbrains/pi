pre-commit:
  parallel: true
  commands:
    format:
      run: bun run format:check
    typecheck:
      run: bun run check

pre-push:
  commands:
    test:
      run: bun run test
    extension-packages:
      run: bash scripts/check-extension-packages.sh
